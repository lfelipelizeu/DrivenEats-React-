export default function Footer () {
    return (
        <footer>
            <button class="disabled" disabled>Selecione os 3 itens para fechar o pedido</button>
            <button class="enabled hidden">Fechar pedido</button>
        </footer>
    );
}